# Production dependencies for Research Knowledge Graph
# Pinned versions for Python 3.11 compatibility

# Core Flask Framework
Flask==3.0.0
Flask-CORS==4.0.0

# WSGI Server for production
gunicorn==21.2.0

# Data Processing - Pinned versions for compatibility
pandas==2.1.4  # Stable version for Python 3.11
numpy==1.24.4  # Compatible with pandas 2.1.4
openpyxl==3.1.2  # Excel file support
xlrd==2.0.1      # Legacy Excel format support

# Graph Processing
networkx==3.2.1

# Utilities
Werkzeug==3.0.1
python-dotenv==1.0.0
python-dateutil==2.8.2

# Security
itsdangerous==2.1.2  # For secure sessions

# Performance & Monitoring (optional but recommended)
flask-compress==1.14  # Gzip compression

# Logging
python-json-logger==2.0.7  # Structured logging